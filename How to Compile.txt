# Compile Bison file:
    bison -d parser.y

# Compile Flex file:
    flex lexer.l

# Create executable from Compiled files:
    gcc parser.tab.c lex.yy.c main.c -o analizador

# Run App
    ./analizador Entrada.usl